// com o código a baixo as rotas estão mapeadas, mas ainda não tem nada implementado, apenas a estrutura para o futuro desenvolvimento
import {express} from 'express';

let router = express.Router();

export default router;